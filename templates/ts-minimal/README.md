# BlitzDB TypeScript minimal template

Scaffolded by `blitz init`. See `blitz.project.json` for the pinned
template/SDK/protocol/server versions this project was resolved against.

Run a server, then `node src/index.ts [port]`.
