# BlitzDB endpoints (TCP for server components, HTTP for the browser).
BLITZ_URL=127.0.0.1:7420
BLITZ_HTTP_URL=http://127.0.0.1:7421
# Optional bearer token (dev server runs open; production: require_auth + row_owner).
BLITZ_TOKEN=
